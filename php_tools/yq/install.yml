---

check: yq --version 2>&1 | grep -E '[0-9][.0-9]+' || (yq --version; false)

all:
  type: pip
  version:
  build_dep: []
  run_dep: []
  flags:
  pre:
  post:

8.4:
  type: pip
  flags: --break-system-packages

8.3:
  type: pip
  flags: --break-system-packages

8.2:
  type: pip
  flags: --break-system-packages

8.1:
  type: pip
  flags: --break-system-packages

8.0:
  type: pip
  flags: --break-system-packages

7.4:
  type: pip
  flags: --break-system-packages

7.3:
  type: pip
  flags: --break-system-packages
